# 2026-06-12 WaveGrad 控制变量调试 List

## 1. 实验目的

以当前“无 Q 网络、无候选评分”的 WaveGrad 版本为基准，采用控制变量法检查 7 个核心参数和 1 个候选评分机制对抓取成功率的影响。

实验由 1 个基准组和 8 个单变量实验组组成，共 9 次完整训练。每个实验组只允许修改表中指定的一项，其余配置必须与基准组一致。

## 2. 统一实验条件

- 所有实验从相同初始状态开始。建议全部从 episode 0 重新训练，不加载旧 checkpoint。
- 所有实验使用相同训练轮数、Webots 世界、初始姿态和安全过滤逻辑。
- 每 500 个 episode 进行一次测试，每次测试保持 200 个有效回合。
- 主要评价指标使用 `test_grasp_success_rate`，辅助观察 `rolling_success_rate_100`、`rolling_mean_return_100` 和安全约束触发情况。
- 不修改奖励函数、Webots 环境、安全过滤、状态输入、网络层数和 replay 逻辑。
- 每次运行前记录实验编号和完整参数，避免仅根据日志文件编号猜测配置。

程序会自动加载 `python_scripts/WaveGrad/checkpoint/catch` 中测试成功率最高的模型。需要从 episode 0 开始时，应先备份并清空该目录中的旧抓取 checkpoint。日志文件不需要删除。

## 3. 参数修改位置

| 参数 | 当前基准值 | 修改文件及位置 | 修改说明 |
|---|---:|---|---|
| 学习率 `lr` | `2e-4` | `python_scripts/WaveGrad/WaveGrad_policy.py`，`WaveGradAgent.__init__()` 中的 `self.lr` | 修改策略网络初始学习率 |
| 去噪步数 `diffusion_steps` | `24` | `python_scripts/WaveGrad/WaveGrad_policy.py`，`WaveGradAgent.__init__()` 创建 `WaveGradPolicy` 的位置 | 修改实际使用的 `diffusion_steps=24`；不要只修改 `WaveGradPolicy` 形参默认值 |
| 更新轮数 `update_epochs` | `3` | `python_scripts/WaveGrad/WaveGrad_policy.py`，`WaveGradAgent.__init__()` 的形参 | 修改每个 episode 的重复更新次数 |
| 探索噪声 `explore_noise_std` | `0.06/0.04/0.02/0.005` | `python_scripts/WaveGrad/WaveGrad_episoid_1.py`，`_catch_train_schedule()` | 按 episode 阶段修改整组噪声 schedule |
| 动作裁剪 `action_clip` | `0.85` | `python_scripts/WaveGrad/WaveGrad_episoid_1.py`，`_catch_train_schedule()` 和 `_catch_eval_schedule()` | 训练与测试必须改成相同值；不要同时修改 `replay_action_clip` |
| 成功经验权重 `success_weight` | `2.0` | `python_scripts/WaveGrad/WaveGrad_policy.py`，`WaveGradAgent.__init__()` 中的 `self.success_weight` | 控制成功轨迹在去噪损失中的强化程度 |
| 失败经验权重 `failure_weight` | `0.75` | `python_scripts/WaveGrad/WaveGrad_policy.py`，`WaveGradAgent.__init__()` 中的 `self.failure_weight` | 控制失败轨迹参与训练的强度 |
| 候选评分机制 | 关闭 | 当前代码中已删除；候选评分实现保存在 `stash@{0}` | 实验时只恢复候选生成和评分相关代码，其他参数仍保持基准值 |

注意：`replay_action_clip=0.85` 是 replay 动作进入训练前的裁剪参数，不属于本轮 `action_clip` 单变量实验。除非单独增加实验，否则保持不变。

## 4. 基准配置

| 配置项 | 基准值 |
|---|---:|
| `lr` | `2e-4` |
| `diffusion_steps` | `24` |
| `update_epochs` | `3` |
| `explore_noise_std` | `0.06 -> 0.04 -> 0.02 -> 0.005` |
| `action_clip` | `0.85` |
| `success_weight` | `2.0` |
| `failure_weight` | `0.75` |
| 候选评分机制 | 关闭 |

基准组必须先完整运行一次，得到当前精简版本自己的成功率。旧 Q 版本的 `60.5%` 不能作为本基准组成绩。

## 5. 八组单变量实验

| 实验组 | 唯一修改项 | 基准值 | 推荐实验值 | 其他参数 |
|---|---|---:|---:|---|
| 实验 1 | `lr` | `2e-4` | `1e-4` | 全部保持基准值 |
| 实验 2 | `diffusion_steps` | `24` | `36` | 全部保持基准值 |
| 实验 3 | `update_epochs` | `3` | `5` | 全部保持基准值 |
| 实验 4 | `explore_noise_std` | `0.06/0.04/0.02/0.005` | `0.08/0.05/0.025/0.005` | 分段边界保持不变 |
| 实验 5 | `action_clip` | `0.85` | `0.75` | 训练和测试同时改为 `0.75` |
| 实验 6 | `success_weight` | `2.0` | `3.0` | 全部保持基准值 |
| 实验 7 | `failure_weight` | `0.75` | `0.60` | 全部保持基准值 |
| 实验 8 | 候选评分机制 | 关闭 | 开启，候选数 `4` | 7 个数值参数必须保持基准值 |

## 6. 候选评分实验注意事项

`stash@{0}` 中保存的候选评分版本同时包含：

- 候选动作数 `4`。
- 候选动作安全过滤和启发式评分。
- `success_weight=3.0`、`failure_weight=0.6` 等其他历史修改。

因此不能直接把整个 stash 当作实验 8。实验 8 只能恢复候选生成、候选评分和候选日志字段，并把 `success_weight`、`failure_weight` 等参数保持为基准值，否则会同时改变多个变量，实验结果无效。

候选评分实验中保持以下评分参数不变：

```text
有效动作权重：2.1
动作变化惩罚：0.08
裁剪惩罚：0.08
动作幅度奖励：0.12
小动作阈值：0.35
小动作惩罚系数：0.35
候选动作数：4
```

本轮只比较“候选评分关闭”和“候选评分开启”，不继续调整评分公式内部参数。

## 7. 实验结果记录表

| 组别 | 唯一修改项 | Episode 500 | Episode 1000 | Episode 1500 | Episode 2000 | 最佳成功率 | 对应 Episode | 备注 |
|---|---|---:|---:|---:|---:|---:|---:|---|
| 基准组 | 无 |  |  |  |  |  |  |  |
| 实验 1 | `lr=1e-4` |  |  |  |  |  |  |  |
| 实验 2 | `diffusion_steps=36` |  |  |  |  |  |  |  |
| 实验 3 | `update_epochs=5` |  |  |  |  |  |  |  |
| 实验 4 | 增大探索噪声 |  |  |  |  |  |  |  |
| 实验 5 | `action_clip=0.75` |  |  |  |  |  |  |  |
| 实验 6 | `success_weight=3.0` |  |  |  |  |  |  |  |
| 实验 7 | `failure_weight=0.60` |  |  |  |  |  |  |  |
| 实验 8 | 开启候选评分 |  |  |  |  |  |  |  |

## 8. 结果判断原则

- 主要比较相同 checkpoint episode 下的 200 回合测试成功率。
- 如果某组只在单次测试中提高，但后续 checkpoint 明显下降，不能直接判断该参数更优。
- 同时检查安全约束、平均回报和动作幅度，防止通过更激进动作短期提高抓取率。
- 完成第一轮 8 组实验后，再选择表现最好的 2 至 3 个参数做第二轮更细粒度取值，不在第一轮组合多个参数。
